import { render, screen } from "@testing-library/react";
import { describe, test } from "vitest";
import React from "react";
import { expect } from "vitest";
import PathPlannerCard from '../../../src/components/cards/PathPlannerCard';
import { EntityType } from "../../../src/api/model";

describe("PathPlannerCard tests", () => {
  // make sure the component renders
  test("render test", () => {
    // render(<PathPlannerCard>hello</PathPlannerCard>);

    // expect(screen.getByText("hello")).toBeDefined();
    
  });
  

});

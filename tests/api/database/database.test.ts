// will return when more time

// import { describe, expect, test } from "vitest";
// import { database, initializeDatabase, selectAllRows } from "../../../src/api/database/database";

// describe("database tests", () => {
//   // note: some of these tests may give false positives on local machines
//   // make sure the CI tests pass; it has to do with your local instantiation of the db
//   test("initialization test", async () => {
//     let results = await initializeDatabase();
//     // only returns on successful init
//     expect(results).toBeTruthy();
//   });

//   test("selectAllRows campaign", async () => {
//     const db = await database;
//     expect(await selectAllRows("campaign")).toBeDefined();
//   });
// });

export const tutorialCampaignId = (id: string): boolean => {
  const tutorialId = "144b4a22-0e15-49f9-adab-a55542b3da36"; // Replace with the ID you want to compare with currentCampaignId

  return id === tutorialId;
};
